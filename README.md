# ninja
Prompt mutation framework for AI red teams. Takes an input prompt and generates 5 semantically equivalent rewrites using distinct obfuscation strategies — synonym swaps, structural rewrites, role injection, circumlocution, and more. Each variant is labeled with the technique used and why it may evade the target classifier. 
